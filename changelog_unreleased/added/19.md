- Implement the upward eng→dir routing edge (R8/R9): an `initiative:challenged` / `initiative:completion-requested` label now yields a proposed dir handback, emitted additively so a feedback-labelled (already-consumed) Initiative is surfaced rather than silently ignored. (#19)
