- Subprocess transport (autonomous `auto_invoke` mode): `orch evaluate --auto-invoke` now actuates routing proposals by ephemerally spawning the actuator shell per task (R1→eng, R8/R9→dir; res never spawned), fire-and-forget, with each run's transcript logged under `--log-dir` (SPEC §5.2) (#25)
